/**
 * The warp table stores a map of all of the rooms which registered warps along with the task name. Using both of these the warp 
 * table obtains the index of the warp in the registry and can return it on request.
 * 
 * @author Sugaku
 */
export class WarpTable {
    
}
